{
  "name": "video-studio",
  "private": true,
  "type": "module",
  "scripts": {
    "web": "node web/server.js",
    "install:whisper": "bash scripts/install-whisper.sh",
    "demo:clip": "ffmpeg -y -f lavfi -i testsrc2=size=1920x1080:rate=30 -f lavfi -i sine=frequency=440 -t 25 -c:v libx264 -pix_fmt yuv420p -g 30 -keyint_min 30 -movflags +faststart -c:a aac assets/clip.mp4"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.1.1",
    "qrcode": "^1.5.4"
  }
}
